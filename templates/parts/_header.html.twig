<header>
  <div class="header-content">
    <div>
      <a href="{{ path('homepage') }}">
        <img class="logo" src="{{ asset('images/logo_brand.png') }}" alt="Logo">
      </a>
    </div>
    <div class="login_wrapper">
      {% if app.user %}
        <p>{{ app.user.lastName }} {{ app.user.firstName }} connecté 🟢&emsp;<a href="{{ path('admin') }}">Administration</a>&emsp;|&emsp;<a href="{{ path('logout') }}">Se déconnecter</a></p>
      {% elseif not app.user %}
        <p>Vous n'êtes pas authentifié 🔴&emsp;<a href="{{ path('login') }}">Se connecter</a></p>
      {% endif %}
    </div>
  </div>
  <div class="header-transition"></div>
</header>