<header>
  <div class="header-content">
    <div class="logo_wrapper">
      <img class="logo" src="{{ asset('images/logo.png') }}" alt="Logo">
      <h1 class="brand-name"><a href="{{ path('homepage') }}">The Spy Company</a></h1>
    </div>
    <div class="login_wrapper">
      {% if app.user %}
        <p>{{ app.user.lastName }} {{ app.user.firstName }} connecté 🟢 <a href="{{ path('logout') }}">Se déconnecter</a></p>
      {% elseif not app.user %}
        <p>Vous n'êtes pas authentifié 🔴 <a href="{{ path('login') }}">Se connecter</a></p>
      {% endif %}
    </div>
  </div>
  <div class="header-transition"></div>
</header>